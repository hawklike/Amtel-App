<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/team_card2"
    style="@style/SimpleCardView"
    android:layout_width="wrap_content"
    android:layout_height="100dp"
    android:minWidth="100dp"
    android:layout_marginEnd="8dp"
    android:elevation="4dp">

    <TextView
        android:id="@+id/team_card_name"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="TK Horní Bludovice"
        android:layout_marginBottom="8dp"
        android:layout_above="@+id/team_card_group"
        android:layout_alignParentTop="true"
        android:textColor="@color/darkGrey"
        android:textSize="16sp" />

    <TextView
        android:id="@+id/team_card_group"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textStyle="bold"
        android:layout_alignParentBottom="true"
        android:text="bez skupiny"
        android:layout_marginEnd="16dp"
        android:textColor="@color/blue"
        android:textSize="24sp" />

</RelativeLayout>